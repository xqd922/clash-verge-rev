## v(7.0.40)

### 修复

- 修复开机自启失效：自启动计划任务被任务管理器、系统清理工具等外部手段删除后，配置仍显示开启但开机不再自启，现每次启动自动对齐配置与系统任务状态，发现脱节即重建任务
- 修复开机启动时核心可能静默失败：系统服务尚未就绪导致核心启动失败时自动重试（3s/8s），不再需要手动打开应用拉起核心



## v(7.0.39)

### 变更

- Windows 自定义标题栏按钮改为微软原生规格：Segoe Fluent 发丝字形（12px）、关闭键悬停系统红 #C42B1C、新增按压态并移除涟漪；标题栏高度调整为 34px
- 默认延迟测试地址由 HTTP 改为 HTTPS（gstatic generate_204），默认超时由 10000ms 降为 2000ms，测速出结果更快
- 杂项设置中未自定义延迟超时时输入框显示灰色占位符 2000，与测试地址框行为一致
- 关闭自动检查更新：启动不再联网检测新版本；手动「检查更新」发现新版时仅在标题栏亮起红色 New 提示，点击后才打开更新弹窗

### 修复

- 修复「Clash 内核」设置行的版本号过长导致换行的问题，改为单行省略显示、悬停可查看完整版本
- 修复「杂项设置」超时输入框始终显示兜底值 10000、清空后无法回到占位状态的问题
- 修复「日志」页空白：本地残留的非法日志等级（如大写 INFO）会导致内核 /logs 订阅被拒且静默重试，现读取时自动归一化为合法等级
- 修复设置页 Clash 开关（如 IPv6）打开页面时先显示默认值再翻转的问题：运行时配置纳入启动预载，首帧即渲染真实状态



## v(7.0.38)

### 变更

- 移除链式代理 UI 功能（相关后端命令、运行时配置更新逻辑与前端界面一并清理）

### 修复

- 修复 Windows 标题栏相关问题：始终以自定义标题栏启动并渲染首帧，避免内容下坠；标题栏切换增加 set-decorations 窗口权限
- 修复首次导入订阅后代理列表为空：导入完成后立即增强配置，节点即时显示



## v(7.0.37)

### 变更

- macOS 使用系统原生标题栏（红黄绿交通灯按钮），Windows/Linux 保留自定义标题栏；设置 → 布局设置新增「优先使用系统标题栏」开关

### 修复

- 修复 macOS/Linux 服务模式安装失败：服务端二进制固定为 v2.3.0，与内置 IPC 客户端版本匹配（此前追踪最新版导致协议不兼容）
- 修复发布流程：交叉编译目标未安装到钉住的 Rust 工具链；prebuild 拉取 GitHub API 加认证与重试

## v(7.0.36)

### 变更

- 应用代码与 v(7.0.35) 保持一致，本次为重新打包：个人版发布流程升级为全平台构建（Windows x64/arm64、macOS x64/arm64、Linux x64/arm64/armhf，含 fixed-webview2 变体）
- macOS 版本未签名，首次安装需右键「打开」绕过 Gatekeeper；各平台自动更新不受影响



### 修复

- 修复开机自启后 TUN 模式未自动生效：开机早期服务仍在启动中，被误判为不可用而临时关闭 TUN
- 修复非管理员 + TUN 场景下核心启动时机：等待服务 IPC 就绪后再启动核心，避免启动失败

## v(7.0.34)

### 修复

- 修复配置切换后节点选择丢失：`activateSelected` 使用闭包中过期的 `profiles` 引用，传入目标 profile UID 确保恢复正确的选择
- 修复 Smart top1 排序：按 Rank 优先级（MostUsed > OccasionalUsed > RarelyUsed）排序后再按 Weight 排序，避免 RarelyUsed 节点（Weight=100 连接成功率）错误显示为 top1
- 修复 Smart 权重过滤：`proxyNames` 为空时返回空结果，不再显示未过滤的旧配置节点
- 配置切换后自动 invalidate `getProfiles` 查询，确保 Smart 权重的 `profileId` 及时更新

## v(7.0.33)

### 修复

- 修复 Smart 权重在配置切换后可能显示旧节点的问题
- 修复延迟检测并发刷新时延迟值闪烁的问题
- Smart 组头和节点卡片只显示当前组内有效的 Top1 权重节点；无有效节点时不显示，避免出现 `Smart - Select` 或不属于该组的节点

### 修改方案

**Smart 权重显示：**
Smart 权重接口可能返回旧配置里的节点，前端现在会用当前组的 `all` 列表过滤权重结果，只保留真实属于当前组的节点。过滤后没有结果时不显示当前节点文本，也不再回退到内核的 `Smart - Select` 占位值。

**延迟刷新：**
避免 `checkListDelay` 和 `delayGroup` 并发写入同一延迟状态时用旧结果覆盖新结果，减少测速时数值闪烁。

## v(7.0.32)

### 修复

- 优化冷启动速度，跳过 service IPC 不存在时的 3 秒等待，核心就绪后立即通知前端
- 修复服务重复重装导致启动慢 4 秒的问题
- 恢复 v1.7.7 的乐观配置切换，跳过子进程验证使切换更快
- 恢复 v1.7.7 的延迟测速体验，移除 5 秒额外 HTTP 超时，结果逐个立即显示

### 修改方案

**冷启动优化（service IPC 白等 3 秒）：**
`wait_for_service_if_needed()` 在 TUN 模式下会轮询 service IPC 是否就绪，每 200ms 一次，最多 3 秒（`SERVICE_WAIT_MAX`）。当 service 根本不存在时（IPC path 不存在），这 3 秒完全是白等。
修复方式：在进入重试循环前检查 `is_service_ipc_path_exists()`，如果 service IPC path 一开始就不存在，直接跳过等待，fallback 到 sidecar 模式。

**冷启动优化（服务重复重装 4 秒）：**
`init_service_manager()` 会调用 `manager.refresh()` 检查服务版本并可能重装（~2 秒）。随后 `init_core_manager()` → `start_core_by_service()` → `run_core_by_service()` 内部无条件再次调用 `SERVICE_MANAGER.lock().await.refresh()`，又触发一次重装（~2 秒）。两次重装总共浪费 4 秒。
修复方式：`run_core_by_service()` 中先检查 `manager.current()` 是否为 `ServiceStatus::Ready`，已就绪则跳过 refresh。同时将 `Handle::refresh_clash()` 从 `futures::join!` 之后移到 `init_core_manager()` 之后，核心就绪后立即通知前端刷新数据。

**配置切换优化（子进程验证）：**
v7.0.31 的 `perform_config_update()` 在每次配置切换时都会 spawn `mihomo -t` 子进程验证配置文件（`validate_config_outcome()`），在 Windows 上耗时 100-500ms。加上两次文件写入（Check + Run），总切换时间 200-900ms。v1.7.7 从不做子进程验证，直接生成配置文件并热重载，切换时间 130-460ms。
修复方式：`perform_config_update()` 跳过 `CoreConfigValidator::validate_config_outcome()` 子进程验证，直接生成 Run 配置文件并调用 `apply_config()` 热重载。增强管线本身已有 YAML 语法检查；如果配置无效，热重载会失败并触发核心重启作为兜底。`apply_generate_config()` 保留给 `runtime.rs` 等需要验证的场景使用。前端同步简化 `activateProfile`，移除 AbortController、序列号跟踪、中断处理等复杂机制，回到 v1.7.7 的简单 await 风格。

**延迟测速优化（HTTP 超时 + UI 更新）：**
v7.0.31 的 mihomo plugin 在每个延迟测试请求上额外加了 5 秒 HTTP 超时（`DEFAULT_REQUEST_TIMEOUT`），导致失败节点等待时间从 `timeout` 变为 `timeout + 5s`。同时前端使用 `requestAnimationFrame` 批量更新延迟结果，每 ~10% 完成才通知一次 UI 刷新，结果分批出现而不是逐个流式显示。
修复方式：`delay_proxy_by_name()` 中 `req_timeout` 改为 `Duration::from_millis(timeout)`（移除 `+ DEFAULT_REQUEST_TIMEOUT`）。前端 `DelayManager` 移除 `requestAnimationFrame` 批量调度逻辑（`scheduleItemFlush`、`scheduleGroupFlush`、`pendingItemUpdates`、`pendingGroupUpdates`），`setDelay()` 改为立即通知所有监听器，`checkListDelay()` 移除 10% 节流，每个节点完成都立即通知组刷新。

## v(7.0.31)

### 修复

- 修复添加订阅时当前使用的订阅变灰并显示切换中断的问题
- 修复 Windows 窗口四个角没有圆角的问题

## v(7.0.30)

### 新增

- 支持在个人版发版流程中锁定 mihomo 核心版本

### 修复

- 修复 mihomo 核心移除 `global-client-fingerprint` 字段后 `BaseConfig` 反序列化失败导致代理页面模式切换和高亮失效的问题
- 修复 Smart 核心返回 PassRule 代理类型导致代理页面空白的问题
- 修复更新按钮位置在 Windows 上与 macOS 不一致的问题
- 修复更新按钮向上偏移的问题
- 修复模式切换（热键/托盘/前端）错误被静默吞掉的问题，改为向前端传播错误并显示通知
- 修复运行时配置不跟踪 mode 变更的问题

### 修改方案

**BaseConfig 反序列化失败（模式切换/高亮失效根因）：**
mihomo 核心新版本移除了 `global-client-fingerprint` 字段（MetaCubeX/mihomo commit bd72c65b），但 `BaseConfig` 结构体仍将 `global_client_fingerprint` 定义为必填 `String`，serde 反序列化时遇到缺失字段直接报错，导致 `GET /configs` 整个响应解析失败。`clashConfig` 始终为 `undefined`，`curMode` 无法获取，按钮不高亮且切换无效果。
修复方式：将 `global_client_fingerprint` 和 `global_ua` 改为 `Option<String>` + `#[serde(default)]`，TypeScript 绑定同步更新为可选字段。

**PassRule 代理类型：**
Smart 核心返回 `PassRule` 类型的代理节点，但 `ProxyType` 枚举中缺少该变体，serde 反序列化失败导致整个代理列表解析出错，代理页面空白。
修复方式：在 `ProxyType` 枚举中添加 `PassRule` 变体。

**模式切换错误静默吞掉：**
`change_clash_mode` 返回类型为 `()`，`patch_clash_mode` Tauri 命令始终返回 `Ok(())`，前端无法感知切换是否成功。
修复方式：`change_clash_mode` 返回 `anyhow::Result<()>`，`patch_clash_mode` 通过 `.stringify_err()` 传播错误，前端增加 try/catch + `showNotice.error` 通知。热键和托盘菜单同步增加错误处理。

**运行时配置不跟踪 mode：**
`PATCH_CONFIG_INNER` 数组只包含 `allow-lan`、`ipv6`、`log-level`、`unified-delay`、`tunnels`，缺少 `mode`，导致模式变更不会同步到运行时配置。
修复方式：在 `PATCH_CONFIG_INNER` 中添加 `"mode"`，并在 `change_clash_mode` 成功后同步更新运行时配置。

## v(7.0.29)

### 修复

- 修复首次切换 Smart 内核时可能因 `Model.bin` 下载不完整或初始化耗时过长导致核心 IPC 等待超时并回滚的问题
- 个人版打包流程会预置 Smart LightGBM 模型，切换 Smart 前会在模型缺失、过小或打包资源更新时自动补齐到应用数据目录
- Smart 核心首次启动等待时间放宽，并在失败提示中附带最近核心日志，便于直接看到模型或核心启动错误

## v(7.0.28)

### 新增

- Smart 策略组展示节点使用频率与权重：折叠时组头显示最常使用的节点及权重，展开后每个节点标注使用频率（最常使用 / 经常使用 / 偶尔使用），数据来自 Smart 内核

## v(7.0.27)

### 修复

- 恢复底部流量图的动画帧率（由 15fps 提升回 30fps），修复流量图滚动不流畅、实时性变差的问题

## v(7.0.26)

### 修复

- 移除系统标题栏切换入口，避免标题栏模式设置造成窗口行为异常
- 修复历史窗口状态中残留的系统标题栏标记被恢复，导致系统标题栏仍然出现的问题
- 自定义标题栏的窗口控制按钮改为原生方形样式，修复关闭按钮 hover 显示为红色圆形的问题
- 关闭 Windows 11 窗口默认圆角，使无系统标题栏时右上角控制按钮保持直角

## v(7.0.25)

### 修复

- 修复个人版发布工作流从指定 tag 构建和重建 release tag 时的处理，确保按触发 tag 产出安装包

### 调整

- 缩小代理页面策略组卡片上下间距，策略组列表显示更紧凑

## v(7.0.24)

### 优化

- 恢复代理组延迟测试的 Mihomo 批量测速快路径，优先使用 `/group/{name}/delay` 一次性获取结果，减少逐节点 IPC 请求带来的等待
- 批量测速结果会直接回填延迟缓存并刷新排序；批量接口失败时仍保留逐节点测速作为兜底

## v(7.0.23)

### 修复

- 修复切换 Smart 内核时可能因首次下载 `ASN.mmdb` 或 Smart 资源初始化超过 10 秒而被提前判失败的问题
- 内核启动就绪检测改为通过 Mihomo IPC `/version` 实际请求确认，避免 Windows named pipe 文件打开探针误判
- 重启内核前后清空 Mihomo IPC 连接池，降低旧连接影响新内核检测和 API 请求的概率
- Smart 自动转换不再默认启用 `prefer-asn`，避免缺少 `ASN.mmdb` 时切换过程被额外下载阻塞

## v(7.0.22)

### 修复

- 修复切换到 Smart 内核后未自动启用 Smart 策略组转换的问题；现在切到 `verge-mihomo-smart` 会在生成运行时配置前启用转换，`url-test` / `fallback` / `load-balance` 组会自动转为 `smart`

## v(7.0.21)

### 修复

- 修复切换 Mihomo / Alpha / Smart 内核时可能把 `verge.yaml` 和运行时配置留在半切换状态的问题；现在新内核启动成功后才提交配置，失败会回滚到原内核
- 修复 Smart 自动转换开关未生效的问题；关闭后不会再暗中把 `url-test` / `fallback` / `load-balance` 组转成 `smart`
- 修复内核切换命令重复重启核心导致体感变慢的问题
- 修复新内核 IPC 未就绪仍被当作切换成功的问题，避免出现切换成功但日志、连接或 API 状态不同步
- 侧车模式停止核心后等待 IPC 释放，降低快速切换时旧进程资源未释放造成的启动异常

### 优化

- 移除前端切换内核后的固定 500ms 等待，切换成功后立即刷新配置和版本状态
- Smart 内核切换后的代理组复位改为后台执行，避免阻塞切换按钮恢复

## v(7.0.20)

### 修复

- 修复日志与连接页面在高频更新时体感不及时的问题
- WebSocket 订阅断线后恢复为更快重连，降低页面切换或连接波动后的等待时间
- 日志自动滚动改为即时跟随，避免平滑滚动在连续日志下产生滞后
- 连接列表刷新时保留最新连接元数据，避免部分连接状态显示沿用旧数据

## v(7.0.19)

### 修复

- 修复开启“静默启动”后，手动双击打开应用也只进入托盘、不显示主界面的问题
- 静默启动现在仅在开机自启入口生效；自动启动会携带 `--autostart` 参数，普通手动启动始终显示主窗口
- Windows 开机自启计划任务新增启动参数，切换“开机自启”或“静默启动”设置时会刷新任务配置

## v(7.0.18)

### 同步上游

- 合并 `upstream/main` 在 2.5.0-rc 至 2.5.1 之间的全部修复与优化（共 174 个提交），下文列出与个人版用户可感知的关键变化

### 性能

- 首页流量图渲染优化：`mousemove` 节流到单帧 RAF、tooltip 仅在形状变化时再触发更新，大幅降低高频鼠标交互下的重绘开销
- 减少流量与内存数据更新的内存分配：移除中间数组拷贝，相同载荷在 50ms 窗口内做签名去重，避免后端高频 push 重复刷新
- WebSocket 订阅引入引用计数与活跃 owner 切换机制，多个组件订阅同一通道时复用同一连接，最后一个组件卸载才真正关闭
- 收窄 app data 全局订阅范围，按消费维度（proxies / rules / clash config / system / uptime / refreshers / 加载状态）拆分 Context，避免无关组件被波及重渲染
- Monaco 编辑器及其 worker / schema / pac 类型改为懒加载，明显缩短冷启动时间

### 修复

- 修复 Windows 上切换节点缓慢的问题
- 修复 Windows 全局代理与 PAC 同时启用时，由于 Sysproxy/Autoproxy 设置顺序导致全局代理被自身清掉的问题（按 sys/auto 启用状态组合决定调用顺序，附带回归测试）
- 修复按住快捷键时连续重复触发的问题（用原子位消抖，按下仅响应首次，松开才复位）
- 修复暂停日志时已有日志被清空的问题（取消订阅但保留视图缓存）
- 修复 TLS 握手失败时无法回退到内置 webpki 根证书的问题，保留 reqwest 完整错误链做关键字判断；订阅下载在系统证书失败时会自动用 webpki 静态根重试一次
- 修复 Wayland (<1.23) 上 webkit dmabuf 渲染异常（在受影响版本上自动禁用 dmabuf）
- 修复 Linux 上窗口在页面加载前出现导致的闪烁
- 修复 macOS 系统托盘网速文字不在点击命中区内的问题
- 修复 hotkey 中 `OS` 键名被原样保留而非解析为 `CMD`
- 修复标题栏拖拽区与窗口控制按钮、其他 hover 区域冲突的问题（拖拽属性挪到内部独立子元素）
- 修复轻量模式退出时误触发清理流程
- 修复禁用系统代理时的守卫逻辑
- 修复订阅 URL 在后端日志中被原文记录的问题（改为脱敏输出）
- 修复 YouTube Premium 解锁检测的判定逻辑
- 修复连接列表列宽控制位置不当的问题
- 修复更新提示弹窗中 GitHub Alert 语法无法渲染、排版不易阅读的问题

### 调整

- 代理组列表新增 sticky scroll：滚动时当前组标题钉在视口顶部，离开范围才下沉
- 代理链路新增告警 tooltip，提示可能的循环引用
- 备份相关对话框统一为应用内 modal 行为，与其他对话框交互一致
- 中间发布产物在上传期间保持为 draft，按 release id 发布以避免冲突

### 重构

- 配置校验结果由 `(bool, String)` 升级为带 kind 的结构化枚举 `ValidationOutcome`（`Valid` / `Invalid { kind, message }` / `Skipped { reason }` / `Busy`），前端可据此做差异化提示与文案分流
- 退出清理逻辑迁移到 `ExitRequested` 事件并通过 `prevent_exit` 异步执行，避免在不可阻止的 `Exit` 阶段被截断
- 简化媒体解锁结果的构造样板代码（新增 `UnlockItem::checked()` 工厂方法）

### 构建

- 对齐 `@tauri-apps/api` / `@tauri-apps/cli` 与 Rust `tauri` crate 至 2.11.0，修复 personal-release 工作流的版本不匹配构建失败
- pre-commit hook 中的 biome 步骤添加 `--no-errors-on-unmatched`，避免暂存被 biome 忽略的文件（如 `Changelog.md`）时 hook 因找不到可处理文件而失败

### 说明

- 本次发布以同步上游为主，未在个人分支引入新的功能定制
- Smart 核心、Personal 发布工作流、SWR 数据层等个人版自有调整保持不变

## v(7.0.17)

### 修复

- 修复代理页面多次点击延迟测速后下方代理组跳动的问题，稳定 Virtuoso 虚拟列表的行 key 策略
- 减少配置页面导入或更新订阅时的跳动刷新，将业务层重试次数从 5 次降至 1 次
- 统一 ProfileItem 组件的数据刷新路径，消除直接调用 `mutate('getProfiles')` 造成的额外刷新
- 稳定 ProfileItem 定时器事件监听的 effect 依赖，减少不必要的事件监听器重建

## v(7.0.16)

### 调整

- 回退配置文件切换链路的定制逻辑，恢复为更接近原项目的切换行为
- 回退 WebView2 / SWR 内存缓存优化相关实现，移除额外的 LRU 缓存提供器接入
- 左下角流量区显示与布局回归原项目，包括流量数值格式恢复为原项目样式

### 修复

- 修复代理首页在路由回退后不显示的问题，恢复 `/` 路由下代理页的正常渲染
- 修复切换回代理页时的页面跳动与重挂载问题，恢复代理页常驻挂载策略并保留滚动状态
- 修复配置页误带回不需要的 `Merge / Script` 两个附加块，恢复为个人版原本的页面结构
- 修复左下角流量显示被改成 `0 B` 的问题，恢复为原项目一致的 `0.00 B` 样式

### 变更明细

- 配置切换相关回退：
  回退 profile 切换链路里的额外并发控制、中断恢复、附加通知时序和代理选择联动恢复逻辑，降低与 `upstream/main` 的长期差异
- 缓存与内存相关回退：
  回退连接/流量 WebSocket 订阅上的额外节流、缓存清理和 SWR 全局 LRU provider 接入，避免继续维护这套个人分支特有实现
- 布局与显示回退：
  回退左下角流量区的显示格式和布局行为，使其重新与原项目保持一致
- 页面结构修正：
  在保持“代理页作为首页”的前提下，补回代理页路由渲染；恢复代理页 keep-alive 以消除切页跳动；同时移除本次回退过程中误带回的配置页附加块

### 说明

- 本版本重点是收缩个人分支与 `upstream/main` 的差异，降低后续持续合并上游更新的成本
- `v7.0.16` 重新发布后，将以本次整理后的正式 release 提交与最新修复为准

## v(7.0.15)

### 新增

- 同步合并原项目 `upstream/main` 的最新功能与修复，并保留个人版产品结构
- 恢复并增强配置编辑器能力，补齐新的文档式编辑器链路和 Monaco 相关支持
- 新增图标缓存与校验能力，补齐 icon 下载、缓存和安全检查相关实现
- 设置页与备份页补齐上游新增能力，包括自动备份、备份历史、WebDAV 相关界面和逻辑

### 优化

- 优化配置页切换流程，减少切换时的重复刷新和阻塞感
- 优化当前配置切换后的代理选择恢复逻辑，切换配置后更快回到目标代理状态
- 优化连接页数据刷新与速度计算逻辑，下载、上传速度显示更稳定
- 优化系统代理状态读取与切换体验，界面状态更接近真实系统状态
- 优化网站连通性与延迟测试逻辑，补齐真实 TLS 延迟与更合理的请求行为
- 优化窗口事件、前端通知和托盘更新链路，减少切换配置或删除订阅后的状态不同步
- 优化日志与错误信息中的敏感 URL 处理，降低日志直接暴露订阅地址的风险

### 修复

- 修复删除订阅后托盘菜单和状态未及时更新的问题
- 修复 Deepin 环境下桌面启动相关问题
- 修复窗口关闭、聚焦和部分监听管理不稳定的问题
- 修复配置切换和配置更新过程中的部分超时、异步竞争和状态残留问题
- 修复网站测试在系统代理关闭时仍错误走代理的问题
- 修复部分代理延迟测试结果不准确、重复请求过多的问题
- 修复图标写入链路中潜在的路径穿越和伪图片写入风险
- 修复前端 lint、typecheck 与合并后 Rust 接口不兼容导致的构建问题

### 个人版保留项

- 继续移除首页，不恢复 `home` 页面
- 继续默认围绕代理页和个人版既有交互组织页面结构
- 继续保留本地 Mihomo path 依赖和个人版版本管理方式
- 继续使用 `merge` 同步上游，不改用 `rebase`

## v(7.0.14)

### 修复

- 修复 URLTest 节点组测速后图钉锁定无法取消的问题，单节点测速和批量测速后都会自动解除固定

### 优化

- 优化配置切换速度，减少切换时的额外开销
- 优化代理延迟测试性能，减少重复请求并更快显示结果

## v(7.0.10)

### ⚡ 优化

- Smart 组标题栏内联显示权重最高节点及占比，替代原有弹窗（Surge 风格）
- 移除 Smart 权重查看弹窗，Smart 组透明化运行
- 修复 Smart 组标题重复显示 "Smart - Select" 的问题

## v(7.0.9)

### ✨ 新功能

- Smart 代理组编辑器新增策略（Strategy）选择器，支持 sticky-sessions / round-robin / least-ping
- Smart 自动转换默认启用 prefer-asn（ASN 偏好）和 sample-rate（采样率）
- Smart 自动转换新增 smart-collector-size（数据收集上限）和 sticky-sessions 默认策略
- Smart 组标题栏内联显示权重 Top 1 节点及占比，替代原有弹窗

### 🐞 修复

- 修复通过下拉框切换 Smart 组节点后未自动 unfix 的问题（handleSelectChange 缺少 groupType 参数）
- 修复 Smart 权重查看器 API 返回数据解析异常的问题
- 修复更新版本号为空时回退下载 URL 失败的问题
- 修复 Smart 组标题显示重复 "Smart - Select" 的问题

### ⚡ 优化

- 移除 Smart 权重弹窗，改为 Surge 风格的透明化设计，权重信息内联展示
- 切换回标准核心时清理 profile 级 smart-collector-size 残留配置

## v(7.0.8)

### 🐞 修复

- 修复批量测速后图钉（固定节点）无法取消的问题（补回 delayGroup API 的 unfixed 副作用）
- 修复切换配置文件再切回后已选节点丢失的问题（activateSelected 使用过时的 SWR 缓存）

### ⚡ 优化

- 批量测速时优先测试当前选中的节点，更快看到结果

## v(7.0.7)

### 🐞 修复

- 修复非管理员启动时 TUN 模式被永久关闭的问题（不再写入配置文件，下次管理员启动仍可正常开启）

## v(7.0.6)

### 🐞 修复

- 修复导入订阅时订阅卡片闪烁的问题（去除重复事件和冗余刷新）
- 降低服务探测失败日志级别，避免未安装服务时控制台刷红色错误

### ⚡ 优化

- 优化批量延迟测试策略：改为逐个测速并即时显示结果，不再全部转圈等到最后
- 移除前端双重超时机制，信任 mihomo 内核超时控制，延迟值更准确
- 批量测速与单独测速使用相同 API，结果一致

## v(7.0.5)

### 🐞 修复

- 修复软件内更新按钮点击无反应的问题（release notes 为空时被错误拦截）
- 修复更新弹窗在下载过程中可被意外关闭的问题
- 修复发布流程中 update.json 缺少更新日志的问题

## v(7.0.4)

### 🐞 修复

- 修复软件内更新按钮点击无反应的问题（release notes 为空时被错误拦截）
- 修复更新弹窗在下载过程中可被意外关闭的问题
- 修复发布流程中 update.json 缺少更新日志的问题

## v(7.0.3)

### 🐞 修复

- 修复切换 Smart 核心后无法连接的问题（等待 IPC 管道就绪后再通知前端）
- 修复代理页面延迟测试按钮点击一次后无法再次触发的问题
- 修复自动更新下载 URL 不匹配导致更新失败的问题
- 修复便携版因检测到已安装版本的服务导致配置路径不匹配的问题

## v(7.0.2)

### ✨ 新功能

- 合并上游 v2.4.6 全部更新
- Masque 协议 GUI 支持
- 流量隧道管理界面，支持可视化添加/删除隧道配置
- 支持订阅设置自动延时监测间隔
- 连接、规则、日志等过滤搜索组件新增清空按钮
- 链式代理增加入口出口与数据流向标识
- 支持 IP 检测自定义 User-Agent
- WebDAV 缓存连接状态并调整自动刷新行为

### 🐞 修复

- 修复 Windows 托盘打开日志失败
- 修复 KDE 首次启动报错（空代理 schema）
- 修复恢复休眠后无法操作托盘
- 修复首次 URL scheme 导入订阅时未及时重载配置
- 修复规则页行号显示逻辑
- 修复 macOS 网口顺序导致无法正确设置代理
- 修复 macOS TUN 覆盖 DNS 使用 114.114.114.114
- 修复无网络时无限请求 IP 归属查询
- 修复 Linux GUI 安装服务模式权限问题
- 修复 eslint.config.ts 未纳入 allowDefaultProject 导致 lint-staged 失败

### 🚀 改进

- 连通性测试默认 URL 保持 http://www.gstatic.com/generate_204
- 优化 IP 信息卡性能与离线状态处理
- winapi 迁移至 windows crate
- 简化托盘事件处理
- 减少 webview 锁竞争
- 文件系统方法迁移为异步
- SWR 用法精简，移除 SWR_REALTIME 配置
- 代理组图标样式美化
- 缩短代理信息获取超时与重试间隔
- 依赖更新：Tauri 2.10.1、swr 2.4.0、react-error-boundary 6.1.1 等

## v(7.0.1)

### 🔧 调整

- 统一延迟开关默认关闭

## v(7.0.0)

### ✨ 新功能

- 支持 Smart (mihomo-smart) 智能代理核心
- Smart 代理组自动转换：使用 Smart 核心时，自动将 url-test/fallback/load-balance 组转换为 smart 类型
- Smart 代理组兼容回退：切回标准核心时，自动将 smart 组恢复为 url-test
- 切换 Smart 核心时自动解除固定节点锁定
- Smart 组不再显示固定图标（由 ML 策略自动选择）

### 🐞 修复

- 修复 Smart 核心切换时配置生成错误
- 修复 mihomo 插件 build.rs 中缺少 Smart 命令注册
- 修复 Rust 缓存导致的 Smart ACL 权限错误

### 🚀 改进

- 移除首页，默认进入代理页面
- 移除全局扩展脚本 (Merge/Script)
- 基于 Clash Verge Rev 精简优化

## v(2.4.6)

> [!IMPORTANT]
> 历经多轮磨合与修正，这是自 2.0 以来我们最满意的里程碑版本。建议所有用户立即升级。

### 🐞 修复问题

- 修复首次启动时代理信息刷新缓慢
- 修复无网络时无限请求 IP 归属查询
- 修复 WebDAV 页面重试逻辑
- 修复 Linux 通过 GUI 安装服务模式权限不符合预期
- 修复 macOS 因网口顺序导致无法正确设置代理
- 修复恢复休眠后无法操作托盘
- 修复首页当前节点图标语义显示不一致
- 修复使用 URL scheme 导入订阅时没有及时重载配置
- 修复规则界面里的行号展示逻辑
- 修复 Windows 托盘打开日志失败
- 修复 KDE 首次启动报错

<details>
<summary><strong> ✨ 新增功能 </strong></summary>

- 升级 Mihomo 内核到最新
- 支持订阅设置自动延时监测间隔
- 新增流量隧道管理界面，支持可视化添加/删除隧道配置
- Masque 协议的 GUI 支持

</details>

<details>
<summary><strong> 🚀 优化改进 </strong></summary>

- 安装服务失败时报告更详细的错误
- 避免脏订阅地址无法 Scheme 导入订阅
- macOS TUN 覆盖 DNS 时使用 114.114.114.114
- 连通性测试替换为更快的 http://1.0.0.1
- 连接、规则、日志等页面的过滤搜索组件新增了清空输入框按钮
- 链式代理增加明显的入口出口与数据流向标识
- 优化 IP 信息卡
- 美化代理组图标样式
- 移除 Linux resources 文件夹下多余的服务二进制文件
## v2.5.1

### 🐞 修复问题

- 备份设置功能异常
- 修复 Windows 节点交互异常

</details>
